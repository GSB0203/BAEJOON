#include<stdio.h>

int main() {
    int arr[31] = {0};
    int num;

    for(int i=0;i<30;i++){
        scanf("%d", &num);
        arr[num]++;
    }

    for(int i=1;i<=30;i++)
        if(arr[i] == 0)
            printf("%d\n", i);

    return 0;
}
