#include<stdio.h>
int main() {
    int n, i;
    int sum = 0;
    int a[101]={};
    scanf("%d", &n);
    
    for(i=0;i<n;i++) {
        scanf("%1d", &a[i]);
        sum += a[i];
    }
    
    printf("%d", sum);
    return 0;
}
