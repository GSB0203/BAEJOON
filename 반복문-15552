#include<stdio.h>
int main()
{
    long long int n, a, b, i;
    scanf("%lld", &n);
    for(i=0;i<n;i++)
    {
        scanf("%lld %lld", &a, &b);
        printf("%lld\n", a+b);
    }
    return 0;
}
