#include<stdio.h>
int main()
{
    int n, arr[101], v, i, sum=0;
    scanf("%d", &n);
    for(i=0;i<n;i++)
        scanf("%d", &arr[i]);
    scanf("%d", &v);
    for(i=0;i<n;i++)
        if(arr[i]==v) sum+=1;
    printf("%d", sum);
        
}
