#include<stdio.h>
int main()
{
    int n, arr[1000000], max=-9999999, min=9999999, i;
    scanf("%d", &n);
    
    for(i=0;i<n;i++)
    {
        scanf("%d", &arr[i]);
        if(arr[i]>max) max=arr[i];
        if(arr[i]<min) min=arr[i];
    }
    printf("%d %d", min, max);
}
