#include<stdio.h>
int main()
{
    int h, m, t;
    scanf("%d %d", &h, &m);
    scanf("%d", &t);
    
    m+=t;
    while(m>=60)
    {
        m-=60;
        h+=1;
    }
    while(h>=24)
        h-=24;
    printf("%d %d", h, m);
}
